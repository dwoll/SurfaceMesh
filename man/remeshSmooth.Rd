% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remeshSmooth.R
\name{remeshSmooth}
\alias{remeshSmooth}
\title{Smooth shape}
\usage{
remeshSmooth(x, indices, nIter = 1L, time = 0.001, normals = FALSE)
}
\arguments{
\item{x}{A \code{CGALmesh} object, i.e., the output of \code{\link[SurfaceMesh]{makeMesh}}.
The mesh must be triangle or be able to be made triangle.}

\item{indices}{Indices of the faces to be smoothed.
If missing, the whole mesh is smoothed.}

\item{nIter}{Positive \code{integer}: Number of iterations.}

\item{time}{Positive number: A time step that corresponds to the speed by
which the surface is smoothed (the larger the faster).
typical values lie between \code{1e-6} and \code{1}. See details.}

\item{normals}{Boolean. Whether to return vertex normals.}
}
\value{
A \code{CGALmesh} object.
}
\description{
Smooths the overall shape of a 3D surface mesh by using
the mean curvature flow.
Includes triangulation if mesh is not already triangle.
}
\details{
See \url{https://doc.cgal.org/latest/PMP_Remeshing/} for details.
}
\examples{
library(SurfaceMesh)
library(rgl)

mesh       <- dataHeart1
mesh_rgl   <- toRGL(mesh)
mesh_s     <- remeshSmooth(mesh, nIter=5, time=1)
mesh_s_rgl <- toRGL(mesh_s)

open3d(windowRect=50 + c(0, 0, 800, 400))
mfrow3d(1, 2)
view3d(0, 0, zoom=0.9)
wire3d(mesh_rgl)
next3d()
view3d(0, 0, zoom=0.9)
wire3d(mesh_s_rgl)

}
\author{
Originally developed by Stephane Laurent, adapted by Daniel Wollschlaeger.
}
