% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hausdorff.R
\name{getHausdorffQuantile}
\alias{getHausdorffQuantile}
\title{Quantile Hausdorff distance between two meshes}
\usage{
getHausdorffQuantile(mesh1, mesh2, symmetric = TRUE, p = 0.95, ...)
}
\arguments{
\item{mesh1}{A \code{CGALmesh} object, i.e., the output of \code{\link[SurfaceMesh]{makeMesh}}.}

\item{mesh2}{A \code{CGALmesh} object, i.e., the output of \code{\link[SurfaceMesh]{makeMesh}}.}

\item{symmetric}{Boolean. Whether to pool the sampled distances from
\code{mesh1} to \code{mesh2} with those from \code{mesh2} to \code{mesh1}
before taking the quantile.}

\item{p}{A number in \eqn{[0, 1]}. The quantile probability, defaults
to \code{0.95}.}

\item{...}{Sampling options passed on to \code{\link[SurfaceMesh]{getSurfaceDist}}.}
}
\value{
A number: the requested quantile of the sampled point-to-mesh
distances. The algorithm uses random sampling, so the result can vary.
}
\description{
Approximate the Hausdorff distance between two meshes using a
quantile (by default the 95th percentile, i.e. "HD95") of the
distances from a random sample of points on one mesh to the other mesh,
instead of their maximum. This is less sensitive to small, isolated
outlying regions than the full Hausdorff distance.
}
\details{
See \url{https://metrics-reloaded.dkfz.de/metric-library/xhd} for details.
Note: This function under the hood simply calls
\code{\link[SurfaceMesh]{getSurfaceDist}} and extracts the quantile Hausdorff distance.
See there for details on the sampling process.
}
\examples{
library(SurfaceMesh)
## symmetric 95\\% Hausdorff distance ("HD95")
getHausdorffQuantile(dataHeart1, dataHeart2, p=0.95)

}
\seealso{
See \code{\link[SurfaceMesh]{getHausdorff}} for the (approximate) Hausdorff distance.
See \code{\link[SurfaceMesh]{getSurfaceDist}} for other surface distance metrics.
}
\author{
Daniel Wollschlaeger.
}
