% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hausdorff.R
\name{getSurfaceDist}
\alias{getSurfaceDist}
\title{Several distance metrics between two meshes}
\usage{
getSurfaceDist(
  mesh1,
  mesh2,
  returnDists = FALSE,
  symmetric = TRUE,
  p = 0.95,
  method = c("random", "grid", "mc"),
  sampleVerts = TRUE,
  sampleEdges = TRUE,
  sampleFaces = TRUE,
  gridSpacing = NULL,
  ptsOnEdges = NULL,
  ptsOnFaces = NULL,
  ptsPerDist = NULL,
  ptsPerEdge = NULL,
  ptsPerArea = NULL,
  ptsPerFace = NULL
)
}
\arguments{
\item{mesh1}{A \code{CGALmesh} object, i.e., the output of \code{\link[SurfaceMesh]{makeMesh}}.}

\item{mesh2}{A \code{CGALmesh} object, i.e., the output of \code{\link[SurfaceMesh]{makeMesh}}.}

\item{returnDists}{Boolean. Return the sampled distances from mesh 1 to 2, and from 2 to 1?}

\item{symmetric}{Boolean. Report the symmetric quantile Hausdorff distance?}

\item{p}{A number in \eqn{[0, 1]}. The quantile probability, defaults
to \code{0.95}.}

\item{method}{\code{character}. \code{"random"} for random uniform sampling,
\code{"grid"} for grid sampling, \code{"mc"} for Monte Carlo sampling.
See details.}

\item{sampleVerts}{Boolean. Do sample vertices?}

\item{sampleEdges}{Boolean. Do sample edges?}

\item{sampleFaces}{Boolean. Do sample faces?}

\item{gridSpacing}{\code{numeric}. The grid spacing for \code{method="grid"}.}

\item{ptsOnEdges}{\code{integer}. For the random sampling method as the
number of points to pick exclusively on edges.
If missing, the number of edges is used.}

\item{ptsOnFaces}{\code{integer}. For the random sampling method as the
number of points to pick on the surface.
If missing, the number of vertices is used.}

\item{ptsPerDist}{\code{numeric}. Points per distance unit. Used for
\code{method="random"} and \code{"mc"} to respectively
determine the total number of points on edges and the number of points per edge.}

\item{ptsPerEdge}{\code{integer}. Points per edge. Used for \code{method="mc"}
as the number of points per edge to pick.}

\item{ptsPerArea}{\code{numeric}. Points per area unit. Used for
\code{method="random"} and \code{"mc"} to respectively determine
the total number of points inside faces and the number of points per face.}

\item{ptsPerFace}{\code{integer}. Points per face. Used for \code{method="mc"}
as the number of points per face to pick.}
}
\value{
A list with components \code{"HDq"} (quantile Hausdorff distance),
\code{"ASSD"} (average symmetric surface distance),
\code{"RMSE"} (root mean squared error).
For \code{returnDists=TRUE} also \code{"dists12"} and \code{"dists21"}.
The algorithm uses random sampling, so the result can vary.
}
\description{
Quantile Hausdorff distance (HD), average symmetric surface distance (ASSD),
and root mean squared error (RMSE) for the surface distance between two meshes.
}
\details{
See \code{\link[SurfaceMesh]{getHausdorffQuantile}} for details on the quantile
Hausdorff distance, \url{https://metrics-reloaded.dkfz.de/metric-library/assd} for ASSD.
Inspired by \url{http://vcglib.net/metro.html}.
For details on sampling options, see
\url{https://doc.cgal.org/latest/Polygon_mesh_processing/group__PMP__distance__grp.html}.
}
\examples{
library(SurfaceMesh)
getSurfaceDist(dataHeart1, dataHeart2, p=0.95, ptsOnFaces=1000L)

}
\seealso{
See \code{\link[SurfaceMesh]{getHausdorff}} for the (approximate) Hausdorff distance,
\code{\link[SurfaceMesh]{getHausdorffQuantile}} for the quantile Hausdorff distance, and
\code{\link[SurfaceMesh]{getJSCDSC}} for volume-overlap based mesh similarity metrics (JSC, DSC).
}
\author{
Daniel Wollschlaeger.
}
