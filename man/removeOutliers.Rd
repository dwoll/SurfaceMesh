% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconstruct.R
\name{removeOutliers}
\alias{removeOutliers}
\title{Remove outliers from point cloud}
\usage{
removeOutliers(x, nNeighbors = 6L, threshPerc, threshDst)
}
\arguments{
\item{x}{Numeric matrix with 3 columns which stores the points, one point per row.}

\item{nNeighbors}{\code{integer}. How many neighboring points to consider
for distance calculation.}

\item{threshPerc}{\code{numeric}. The maximum percentage of points to remove.
If specified, \code{threshDst} should not be specified, otherwise
\code{threshDst} takes precedence.}

\item{threshDst}{\code{numeric}. The minimum distance for a point to be
considered as outlier. If missing and \code{threshPerc} is missing as well,
twice the average spacing is chosen as a reasonable default value.}
}
\value{
A numeric matrix of points with outliers removed.
}
\description{
Remove outliers from point cloud based on their distance to
neighboring points.
}
\details{
See \url{https://doc.cgal.org/latest/Point_set_processing_3/}
for details.
}
\examples{
library(SurfaceMesh)
library(rgl)

ptsBefore   <- matrix(rnorm(120L, mean=0, sd=1), ncol=3L)
(avgSpacing <- getAverageSpacing(ptsBefore, nNeighbors=6L))
## contaminate
idx              <- sample(seq_len(nrow(ptsBefore)), 20L, replace=FALSE)
ptsBefore[idx, ] <- matrix(rnorm(5L, mean=10, sd=1), ncol=3L)
ptsAfter         <- removeOutliers(ptsBefore, nNeighbors=6L)
dim(ptsBefore)
dim(ptsAfter)

}
\seealso{
\code{\link[SurfaceMesh]{reconstructAFS}},
\code{\link[SurfaceMesh]{reconstructPoisson}},
\code{\link[SurfaceMesh]{reconstructSSS}},
\code{\link[SurfaceMesh]{alphaWrap}}
}
