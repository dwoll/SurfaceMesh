% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh.R
\name{plotEdges}
\alias{plotEdges}
\title{Plot some edges}
\usage{
plotEdges(
  x,
  edges,
  color = "black",
  lwd = 2,
  edgesAsTubes = TRUE,
  tubesRadius = 0.03,
  verticesAsSpheres = TRUE,
  only,
  spheresRadius = 0.05,
  spheresColor = color
)
}
\arguments{
\item{x}{A matrix with 3 columns giving the coordinates of the vertices.}

\item{edges}{\code{integer} matrix with two columns giving the edges by pairs of
vertex indices.}

\item{color}{\code{character}. Color for the edges.}

\item{lwd}{Positive number. Line width. Ignored if \code{edgesAsTubes=TRUE}.}

\item{edgesAsTubes}{Boolean. Draw edges as tubes?}

\item{tubesRadius}{Positive number. Radius of the tubes when \code{edgesAsTubes=TRUE}.}

\item{verticesAsSpheres}{Boolean. Draw vertices as spheres?}

\item{only}{\code{integer} vector with the indices of the vertices
to plot as spheres. If missing, all vertices are plotted as spheres.}

\item{spheresRadius}{The radius of the spheres when
\code{verticesAsSpheres=TRUE}.}

\item{spheresColor}{\code{character}. Color of the spheres when
\code{verticesAsSpheres=TRUE}.}
}
\value{
No value.
}
\description{
Plot the given edges with functions from package \strong{rgl}.
}
\examples{
library(SurfaceMesh)
library(rgl)
# triangulate and plot the pentagrammic prism mesh
mesh     <- makeMesh(dataPentaPrism, triangulate=TRUE)
mesh_rgl <- toRGL(mesh)
open3d(windowRect=c(50, 50, 562, 562), zoom=0.9)
shade3d(mesh_rgl, color="navy")
# plot the exterior edges only, given in `mesh[["exteriorEdges"]]`
plotEdges(mesh[["vertices"]],
          mesh[["exteriorEdges"]],
          color        ="gold",
          tubesRadius  =0.02,
          spheresRadius=0.02)

}
\author{
Originally developed by Stephane Laurent, adapted by Daniel Wollschlaeger.
}
