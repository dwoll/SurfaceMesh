% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boolean.R
\name{boolIntersection}
\alias{boolIntersection}
\title{Intersection of meshes}
\usage{
boolIntersection(x, repairSoup = TRUE, normals = FALSE, verbose = FALSE)
}
\arguments{
\item{x}{A list of meshes, each being either a \code{\link[rgl]{mesh3d}} object
from package \strong{rgl}, or a \code{CGALmesh} object,
i.e., the output of \code{\link[SurfaceMesh]{makeMesh}}.}

\item{repairSoup}{Boolean. Clean the meshes (merging
duplicated vertices, duplicated faces, removing isolated vertices)?
Set to \code{FALSE} if you know the meshes are clean, to
gain some speed.}

\item{normals}{Boolean. Return vertex normals of the output mesh?}

\item{verbose}{Boolean. Print out messages about mesh processing?}
}
\value{
A \code{CGALmesh} object.
}
\description{
Compute the intersection of the given meshes.
}
\details{
See \url{https://doc.cgal.org/latest/PMP_Boolean_operations/} for details.
}
\examples{
library(SurfaceMesh)
library(rgl)
# mesh one: truncated icosahedron; we triangulate it
mesh1 <- makeMesh(dataTruncIcosahedron,
                  triangulate=TRUE,
                  normals    = FALSE)

# mesh two: a cube
mesh2_rgl <- translate3d(cube3d(), 2, 0, 0)
mesh2     <- makeMesh(mesh2_rgl,
                      triangulate=TRUE,
                      normals    =FALSE)

# compute the intersection
mesh_i <- boolIntersection(list(mesh1, mesh2))

# plot
mesh1_rgl  <- toRGL(mesh1)
mesh_i_rgl <- toRGL(mesh_i)
open3d(windowRect=c(50, 50, 562, 562))
shade3d(mesh1_rgl,  color="yellow", alpha=0.2)
shade3d(mesh2_rgl,  color="cyan",   alpha=0.2)
shade3d(mesh_i_rgl, color="red")
plotEdges(vertices         =mesh_i[["vertices"]],
          edges            =mesh_i[["exteriorEdges"]],
          edgesAsTubes     =FALSE,
          lwd              =3,
          verticesAsSpheres=FALSE)

}
\seealso{
See also \code{\link[SurfaceMesh]{boolUnion}} and
\code{\link[SurfaceMesh]{boolDifference}} for other Boolean mesh operations.
}
\author{
Originally developed by Stephane Laurent, adapted by Daniel Wollschlaeger.
}
