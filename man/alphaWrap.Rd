% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alphaWrap.R
\name{alphaWrap}
\alias{alphaWrap}
\title{3D alpha wrapping}
\usage{
alphaWrap(x, alphaRel, offsetRel, normals = FALSE)
}
\arguments{
\item{x}{A \code{CGALmesh} object, i.e.,
the output of \code{\link[SurfaceMesh]{makeMesh}}, or a numeric matrix
with 3 columns which stores the points, one point per row.}

\item{alphaRel}{Relative alpha parameter. The actual alpha parameter (see
details) is defined as the length of the diagonal of the bounding box of
the point cloud divided by the relative alpha parameter. Increase for
a more detailed mesh.}

\item{offsetRel}{Relative offset. The actual offset parameter (see details)
is defined as the length of the diagonal of the bounding box of the
point cloud divided by the relative offset parameter. Increase for
output that is closer to input mesh.}

\item{normals}{Boolean. Return vertex normals?}
}
\value{
A \code{CGALmesh} object.
}
\description{
Reconstruction of a surface mesh from a cloud of 3D points
by alpha wrapping.
}
\details{
See \url{https://doc.cgal.org/latest/Alpha_wrap_3/} for details.
}
\examples{
library(SurfaceMesh)
library(rgl)
mesh        <- dataHeart1
mesh_rgl    <- toRGL(mesh)
mesh_aw     <- alphaWrap(mesh,
                         alphaRel =10,
                         offsetRel=300)
mesh_aw_rgl <- toRGL(mesh_aw)

mfrow3d(1, 2)
view3d(0, -90, zoom=0.7)
shade3d(mesh_rgl, col="gray")
next3d()
view3d(0, -90, zoom=0.7)
shade3d(mesh_aw_rgl, col="gray")

}
\seealso{
See also \code{\link[SurfaceMesh]{reconstructAFS}},
\code{\link[SurfaceMesh]{reconstructSSS}},
\code{\link[SurfaceMesh]{reconstructPoisson}} for other surface reconstruction
methods.
}
\author{
Originally developed by Stephane Laurent, adapted by Daniel Wollschlaeger.
}
