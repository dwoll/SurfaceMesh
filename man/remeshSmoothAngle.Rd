% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remeshSmooth.R
\name{remeshSmoothAngle}
\alias{remeshSmoothAngle}
\title{Smoothing by angle optimization}
\usage{
remeshSmoothAngle(
  x,
  dihedralAngle = 60,
  nIter = 1L,
  useSafeConstr = FALSE,
  doProject = TRUE,
  normals = FALSE
)
}
\arguments{
\item{x}{A \code{CGALmesh} object, i.e., the output of \code{\link[SurfaceMesh]{makeMesh}}.
The mesh must be triangle or be able to be made triangle.}

\item{dihedralAngle}{Positive number. Constrain edges with a dihedral
angle over given value.}

\item{nIter}{Positive \code{integer}: Number of iterations.}

\item{useSafeConstr}{Boolean. Use safety constraints for moving vertices?}

\item{doProject}{Boolean. Project points onto initial surface after
each iteration?}

\item{normals}{Boolean. Return vertex normals?}
}
\value{
A \code{CGALmesh} object.
}
\description{
Smoothes a 3D surface mesh by angle optimization.
Includes triangulation if mesh is not already triangle.
}
\details{
See \url{https://doc.cgal.org/latest/PMP_Remeshing/} for details.
Note that CGAL \code{angle_and_area_smoothing()} supports smoothing by area
optimization as well. However, this feature requires the external
Ceres library (\url{http://ceres-solver.org/}) and thus is currently
not enabled here.
}
\examples{
library(SurfaceMesh)
library(rgl)

mesh       <- makeMesh(dataSeptuaginta, triangulate=TRUE)
mesh_rgl   <- toRGL(mesh)
mesh_s     <- remeshSmoothAngle(mesh, nIter=5L)
mesh_s_rgl <- toRGL(mesh_s)

mfrow3d(1, 2)
view3d(45, -45, zoom=0.7)
shade3d(mesh_rgl, col="gray")
next3d()
view3d(45, -45, zoom=0.7)
shade3d(mesh_s_rgl, col="gray")

}
\seealso{
See  \code{\link[SurfaceMesh]{remeshIsotropic}},
\code{\link[SurfaceMesh]{remeshSimplify}},
\code{\link[SurfaceMesh]{remeshSmoothShape}},
\code{\link[SurfaceMesh]{remeshSmoothTangentRelax}}
for other remeshing operations.
}
