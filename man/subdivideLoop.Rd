% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subdivide.R
\name{subdivideLoop}
\alias{subdivideLoop}
\title{Loop subdivision and deformation}
\usage{
subdivideLoop(x, nIter = 1L, normals = FALSE)
}
\arguments{
\item{x}{A \code{CGALmesh} object, i.e., the output of \code{\link[SurfaceMesh]{makeMesh}}.
The mesh must be triangle or be able to be made triangle.}

\item{nIter}{Positive \code{integer}: Number of iterations.}

\item{normals}{Boolean. Whether to return vertex normals.}
}
\value{
A \code{CGALmesh} object.
}
\description{
Performs the 'Loop' subdivision and deformation.
Includes triangulation if mesh is not already triangle.
}
\details{
See \url{https://doc.cgal.org/latest/Subdivision_method_3/} for details.
}
\examples{
library(SurfaceMesh)
library(rgl)

mesh       <- makeMesh(dataPentaPrism, triangulate=TRUE)
mesh_rgl   <- toRGL(mesh)
mesh_l     <- subdivideLoop(mesh, nIter=2)
mesh_l_rgl <- toRGL(mesh_l)

open3d(windowRect=50 + c(0, 0, 800, 400))
mfrow3d(1, 2)
view3d(0, 0, zoom=0.9)
wire3d(mesh_rgl)
next3d()
view3d(0, 0, zoom=0.9)
wire3d(mesh_l_rgl)

}
\author{
Originally developed by Stephane Laurent, adapted by Daniel Wollschlaeger.
}
